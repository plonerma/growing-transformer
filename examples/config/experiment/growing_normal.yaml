# @package _global_

defaults:
  - _self_
  - override /datasets:
      - wiki_big
  - override /model: grow_start
  - override /training: growing

training:
  schedule:
    training_epochs: 3

  grow_tune_params:
    lr: 1e-5
