# @package _global_

defaults:
  - override /datasets:
    - wiki_big
  - override /model: grow_start
  - override /training: growing
  - override /training/schedule: growing_single_step
  - _self_



training:
  schedule:
    training_epochs: 10

  grow_tune_params:
    learning_rate: 1e-5

  grow_data_portion: 1

load_state: "${hydra:runtime.cwd}/models/grow_start.pt"
save_model: False
