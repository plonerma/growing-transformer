# @package _global_

defaults:
  - override /datasets:
    - wiki_big
    - bookcorpus
  - override /model: huggingface
  - override /training: static
  - _self_

training:
  batch_size: 16
  gca_batches: 16
  learning_rate: 5e-5
  weight_decay: 0.01
  betas: [0.9, 0.999]
