# @package _global_

defaults:
  - override /datasets:
    - wiki_big
    - bookcorpus
  - override /model: huggingface
  - override /training: static
  - _self_

training:
  batch_size: 128
  gca_batches: 2
  learning_rate: 1e-4
  weight_decay: 0.01
  betas: [0.9, 0.999]
